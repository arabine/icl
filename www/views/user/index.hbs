<div class="container-fluid">

	{{> user_topics}}
    
    <!-- Main page -->
    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
          <h1 class="page-header">Paramètres du compte</h1>

          	{{#if failure}}
				<div id="error-box" class="alert alert-danger">{{failure}}</div>
			{{else}}
				{{#if success}}
				<div id="success-box" class="alert alert-success">{{success}}</div>
				{{/if}}
			{{/if}}

          	<div class ="row">
          		<div class="col-sm-3">

		          	<div class="panel panel-primary">
					    <div class="panel-heading">
					        <h3 class="panel-title">Utilisateur {{username}}</h3>
					    </div>
					    <div class="panel-body">
						    <p>{{nickname}} ({{role}})</p>
						    <p>{{email}}</p>
							<p>
							<script>
							document.write("<img src=\"" + GetGravatar('{{email}}') + "\" />");
							</script>
							</p>
						</div>
					</div>
				</div>

				<div class="col-sm-6">

		          	<div class="panel panel-primary">
					    <div class="panel-heading">
					        <h3 class="panel-title">Modifier les paramètres du compte</h3>
					    </div>
					    <div class="panel-body">
						    <form id="userform" class="form-horizontal" role="form" action="/user" method="post">

						    	<input type="hidden" name="_csrf" value="{{csrf}}">

						    	<p>Par défaut, votre pseudonyme est le nom d'utilisateur. Vous pouvez spécifier un pseudonyme différent.</p>
								<div style="margin-bottom: 25px" class="input-group">
									<span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
									<input id="user-nickname" type="text" class="form-control" name="nickname" value="" placeholder="{{nickname}}" />
								</div>

								<p>TarotClub supporte les <a href="https://fr.gravatar.com/" target="_blank">Gravatars</a> automatiquement en utilisant votre adresse mail.</p>
								<p>Sinon, indiquez l'emplacement de votre image (adresse Internet).</p>
								<div style="margin-bottom: 25px" class="input-group">
									<span class="input-group-addon"><i class="glyphicon glyphicon-picture"></i></span>
									<input id="user-avatar" type="text" class="form-control" name="avatar" value="" placeholder="{{avatar}}" />
								</div>

								<div style="margin-bottom: 25px" class="input-group">
									<span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
									<input id="signup-password" type="password" class="form-control" name="password" placeholder="Mot de passe" />
								</div>

								<div style="margin-top:10px" class="form-group">
									<div class="col-sm-12 controls">
									<input type="submit" value="Sauver" name="submit" class="btn btn-primary" />
									</div>
								</div>

							</form>
						</div>
					</div>
				</div>

			</div> <!-- Row -->

    </div>
</div> <!-- Container -->
